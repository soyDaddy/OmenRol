<%- include('partials/header', { title: 'Guía de Uso' }) %>

<div class="max-w-6xl mx-auto px-4 pt-8 pb-16">
  <!-- Cabecera de la página -->
  <div class="mb-12 text-center">
    <h1 class="text-4xl font-bold mb-4">Guía de Uso</h1>
    <p class="text-lg text-gray-600 max-w-3xl mx-auto">Aprende a utilizar todas las funcionalidades de OmenRol en tu servidor de Discord y saca el máximo provecho del sistema de rol, minijuegos y opciones de personalización.</p>
  </div>

  <!-- Navegación de la guía -->
  <div class="sticky top-4 z-10 bg-white shadow rounded-lg p-4 mb-8 flex overflow-x-auto">
    <nav class="flex space-x-4" id="guide-nav">
      <a href="#inicio" class="nav-link text-indigo-600 hover:text-indigo-500 px-3 py-2 font-medium text-sm rounded-md whitespace-nowrap active">Primeros pasos</a>
      <a href="#comandos" class="nav-link text-gray-600 hover:text-indigo-500 px-3 py-2 font-medium text-sm rounded-md whitespace-nowrap">Comandos básicos</a>
      <a href="#sistema-rol" class="nav-link text-gray-600 hover:text-indigo-500 px-3 py-2 font-medium text-sm rounded-md whitespace-nowrap">Sistema de rol</a>
      <a href="#minijuegos" class="nav-link text-gray-600 hover:text-indigo-500 px-3 py-2 font-medium text-sm rounded-md whitespace-nowrap">Minijuegos</a>
      <a href="#faq" class="nav-link text-gray-600 hover:text-indigo-500 px-3 py-2 font-medium text-sm rounded-md whitespace-nowrap">FAQ</a>
    </nav>
  </div>

  <!-- Contenido de la guía -->
  <div class="bg-white shadow rounded-lg overflow-hidden">
    <!-- Primeros pasos -->
    <section id="inicio" class="guide-section border-b border-gray-200">
      <div class="p-8">
        <h2 class="text-2xl font-bold mb-6">Primeros pasos con OmenRol</h2>
        
        <div class="grid md:grid-cols-3 gap-8">
          <div class="bg-gray-50 p-6 rounded-lg">
            <div class="flex items-center justify-center w-12 h-12 rounded-full bg-indigo-100 text-indigo-600 mb-4">
              <i class="fas fa-plus-circle text-xl"></i>
            </div>
            <h3 class="text-lg font-semibold mb-2">1. Añadir el bot</h3>
            <p class="text-gray-600 mb-4">Invita a OmenRol a tu servidor de Discord usando el botón "Invitar" en nuestra página principal.</p>
            <a href="/invite" class="text-indigo-600 hover:text-indigo-500 font-medium">Añadir bot <i class="fas fa-arrow-right ml-1"></i></a>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <div class="flex items-center justify-center w-12 h-12 rounded-full bg-indigo-100 text-indigo-600 mb-4">
              <i class="fas fa-cog text-xl"></i>
            </div>
            <h3 class="text-lg font-semibold mb-2">2. Configuración inicial</h3>
            <p class="text-gray-600 mb-4">Usa el comando <code>!setup</code> en tu servidor o accede al panel web para configurar los ajustes básicos.</p>
            <a href="/servers" class="text-indigo-600 hover:text-indigo-500 font-medium">Panel de control <i class="fas fa-arrow-right ml-1"></i></a>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <div class="flex items-center justify-center w-12 h-12 rounded-full bg-indigo-100 text-indigo-600 mb-4">
              <i class="fas fa-terminal text-xl"></i>
            </div>
            <h3 class="text-lg font-semibold mb-2">3. Explorar comandos</h3>
            <p class="text-gray-600 mb-4">Usa <code>!help</code> para ver la lista de comandos disponibles y empezar a usar el bot.</p>
            <a href="/commands" class="text-indigo-600 hover:text-indigo-500 font-medium">Ver comandos <i class="fas fa-arrow-right ml-1"></i></a>
          </div>
        </div>
        
        <div class="mt-8 p-4 bg-blue-50 rounded-lg">
          <div class="flex">
            <div class="flex-shrink-0">
              <i class="fas fa-info-circle text-blue-600 text-xl"></i>
            </div>
            <div class="ml-4">
              <h4 class="text-lg font-medium text-blue-800">Personalización del bot</h4>
              <p class="text-blue-700 mt-1">El prefijo predeterminado es <code>!</code> pero puedes cambiarlo usando <code>!setprefix [nuevo_prefijo]</code>. También puedes configurar canales específicos, roles administrativos y personalizar el idioma desde el panel web.</p>
            </div>
          </div>
        </div>
      </div>
    </section>
    
    <!-- Minijuegos -->
    <section id="minijuegos" class="guide-section border-b border-gray-200 hidden">
      <div class="p-8">
        <h2 class="text-2xl font-bold mb-6">Minijuegos de OmenRol</h2>
        
        <p class="text-gray-600 mb-6">OmenRol incluye varios minijuegos para mantener activa y entretenida tu comunidad:</p>
        
        <div class="grid md:grid-cols-2 gap-6">
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-3">🎰 Tragaperras (Slots)</h3>
            <p class="text-gray-600 mb-4">Juega a las tragaperras y prueba tu suerte.</p>
            <p class="text-sm font-medium">Comando: <code>/slots <cantidad> [modo]</code></p>
            <p class="text-xs text-gray-500 mt-2">Modos disponibles: Clásico (3 rodillos), Premium (5 rodillos).</p>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-3">🔼 Alta o Baja</h3>
            <p class="text-gray-600 mb-4">Juega a Alta o Baja y multiplica tus monedas.</p>
            <p class="text-sm font-medium">Comando: <code>/altabaja <cantidad> [rondas]</code></p>
            <p class="text-xs text-gray-500 mt-2">Rondas disponibles: 1-10 (por defecto 5).</p>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-3">🪙 Lanzamiento de Moneda (Coinflip)</h3>
            <p class="text-gray-600 mb-4">Lanza una moneda y apuesta contra la CPU o contra otro jugador.</p>
            <p class="text-sm font-medium">Comando: <code>/coinflip <cantidad> <modo> [lado]</code></p>
            <p class="text-xs text-gray-500 mt-2">Modos disponibles: VS CPU, VS Jugador.</p>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-3">🏰 Torre de Riesgo</h3>
            <p class="text-gray-600 mb-4">Sube los niveles de la torre y gana más monedas cuanto más alto llegues.</p>
            <p class="text-sm font-medium">Comando: <code>/torre <cantidad> <dificultad></code></p>
            <p class="text-xs text-gray-500 mt-2">Dificultades disponibles: Novato, Aventurero, Temerario, Legendario.</p>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-3">🃏 Blackjack</h3>
            <p class="text-gray-600 mb-4">Juega al Blackjack contra la casa y gana monedas.</p>
            <p class="text-sm font-medium">Comando: <code>/blackjack <cantidad></code></p>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-3">🎡 Ruleta</h3>
            <p class="text-gray-600 mb-4">Juega a la ruleta de colores y multiplica tus monedas.</p>
            <p class="text-sm font-medium">Comando: <code>/ruleta [cantidad] [color] [dificultad]</code></p>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-3">🎲 Dados</h3>
            <p class="text-gray-600 mb-4">Juega a los dados y gana monedas según tu predicción.</p>
            <p class="text-sm font-medium">Comando: <code>/dados simple <cantidad> <número></code></p>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-3">✂️ Piedra, Papel o Tijera</h3>
            <p class="text-gray-600 mb-4">Juega a piedra, papel o tijera contra la CPU o contra otro jugador.</p>
            <p class="text-sm font-medium">Comando: <code>/ppt <cantidad> <modo> [rondas]</code></p>
          </div>
        </div>
      </div>
    </section>
    
    <!-- FAQ -->
    <section id="faq" class="guide-section border-b border-gray-200 hidden">
      <div class="p-8">
        <h2 class="text-2xl font-bold mb-6">Preguntas frecuentes</h2>
        
        <div class="space-y-6">
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-2">¿Cómo puedo añadir OmenRol a mi servidor?</h3>
            <p class="text-gray-600">Para añadir OmenRol a tu servidor, simplemente haz clic en el botón "Invitar" en nuestra página principal y selecciona el servidor donde quieres añadirlo. Necesitarás tener permisos de administrador en el servidor.</p>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-2">¿Qué sistemas de rol soporta OmenRol?</h3>
            <p class="text-gray-600">OmenRol incluye un sistema de rol propio que es altamente personalizable. Puedes adaptar las razas, clases, estadísticas y mecánicas para que se asemejen a diferentes sistemas como D&D, Pathfinder, o crear uno completamente original. Los administradores pueden configurar todos estos aspectos desde el panel web.</p>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-2">¿Cuántos personajes puede tener cada usuario?</h3>
            <p class="text-gray-600">Por defecto, cada usuario puede tener hasta 3 personajes por servidor. Los administradores pueden aumentar este límite desde el panel de configuración hasta un máximo de 10 personajes por usuario.</p>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-2">¿Cómo puedo crear misiones personalizadas?</h3>
            <p class="text-gray-600">Los administradores pueden crear misiones personalizadas desde el panel web, en la sección "Creador de misiones". Allí podrás configurar las etapas, desafíos, diálogos y recompensas de cada misión. También puedes agrupar misiones para crear aventuras completas.</p>
          </div>
          
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-lg font-semibold mb-2">¿El bot funciona en canales privados?</h3>
            <p class="text-gray-600">Sí, OmenRol puede funcionar en cualquier canal donde tenga permisos para leer y enviar mensajes. Puedes configurar canales específicos para diferentes funcionalidades desde el panel de administración, como un canal para combates, otro para tiendas, etc.</p>
          </div>
        </div>
        
        <div class="mt-8 p-4 bg-purple-50 rounded-lg">
          <div class="flex">
            <div class="flex-shrink-0">
              <i class="fas fa-question-circle text-purple-600 text-xl"></i>
            </div>
            <div class="ml-4">
              <h4 class="text-lg font-medium text-purple-800">¿Necesitas más ayuda?</h4>
              <p class="text-purple-700 mt-1">Si tienes más preguntas o necesitas soporte técnico, únete a nuestro <a href="https://discord.gg/omenrol" class="underline">servidor oficial de Discord</a> o contacta con nosotros a través del <a href="/contact" class="underline">formulario de contacto</a>.</p>
            </div>
          </div>
        </div>
      </div>
    </section>
    
    <!-- Pie de página -->
    <section class="bg-gray-50">
      <div class="p-8 text-center">
        <h2 class="text-2xl font-bold mb-6">¡Comienza la aventura con OmenRol!</h2>
        
        <p class="text-gray-600 mb-8 max-w-3xl mx-auto">Transforma tu servidor de Discord en un auténtico mundo de rol y aventuras con todas las herramientas que necesitas para crear experiencias inolvidables.</p>
        
        <div class="flex flex-col sm:flex-row justify-center space-y-4 sm:space-y-0 sm:space-x-4">
          <a href="/invite" class="px-6 py-3 bg-indigo-600 text-white rounded-md hover:bg-indigo-500 transition-colors font-medium">
            <i class="fas fa-plus-circle mr-2"></i> Añadir a Discord
          </a>
          
          <a href="/servers" class="px-6 py-3 bg-white border border-gray-300 text-gray-700 rounded-md hover:bg-gray-50 transition-colors font-medium">
            <i class="fas fa-cog mr-2"></i> Panel de control
          </a>
          
          <a href="https://discord.gg/omenrol" class="px-6 py-3 bg-white border border-gray-300 text-gray-700 rounded-md hover:bg-gray-50 transition-colors font-medium">
            <i class="fa-brands fa-discord mr-2"></i> Comunidad
          </a>
        </div>
      </div>
    </section>
    
    <!-- Script para la navegación -->
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        // Inicialización: mostrar solo la primera sección
        document.querySelector('.guide-section').classList.remove('hidden');
        
        // Función para cambiar de sección
        function changeSection(sectionId) {
          // Ocultar todas las secciones
          document.querySelectorAll('.guide-section').forEach(section => {
            section.classList.add('hidden');
          });
          
          // Mostrar la sección seleccionada
          document.getElementById(sectionId).classList.remove('hidden');
          
          // Actualizar clases de navegación
          document.querySelectorAll('.nav-link').forEach(link => {
            link.classList.remove('active', 'text-indigo-600');
            link.classList.add('text-gray-600');
          });
          
          // Marcar como activo el enlace seleccionado
          document.querySelector(`a[href="#${sectionId}"]`).classList.add('active', 'text-indigo-600');
          document.querySelector(`a[href="#${sectionId}"]`).classList.remove('text-gray-600');
          
          // Desplazarse al inicio de la sección
          window.scrollTo({
            top: document.querySelector('.bg-white.shadow').offsetTop - 80,
            behavior: 'smooth'
          });
        }
        
        // Añadir eventos de clic a los enlaces de navegación
        document.querySelectorAll('.nav-link').forEach(link => {
          link.addEventListener('click', function(e) {
            e.preventDefault();
            const sectionId = this.getAttribute('href').substring(1);
            changeSection(sectionId);
            
            // Actualizar URL con hash
            history.pushState(null, null, `#${sectionId}`);
          });
        });
        
        // Gestionar navegación por hash en la URL
        if (window.location.hash) {
          const sectionId = window.location.hash.substring(1);
          if (document.getElementById(sectionId)) {
            changeSection(sectionId);
          }
        }
        
        // Gestionar navegación mediante botones de adelante/atrás
        window.addEventListener('popstate', function() {
          if (window.location.hash) {
            const sectionId = window.location.hash.substring(1);
            if (document.getElementById(sectionId)) {
              changeSection(sectionId);
            }
          } else {
            // Si no hay hash, mostrar la primera sección
            changeSection('inicio');
          }
        });
      });
    </script>

<%- include('partials/footer') %>